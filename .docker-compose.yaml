version: "3"

services:
  websocket:
    build:
      context: ./Websocket
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
      
  dashboard:
    build:
      context: ./Dashboard
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
